/**
 * Ersatz für das Paket `server-only` in Tests. Übernommen aus
 * PythonPfad/SQLPfad: `server-only` wirft beim Import außerhalb einer
 * Server-Umgebung; der Ersatz macht die Module in Vitest importierbar.
 */
export {};
